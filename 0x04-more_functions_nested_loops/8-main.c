#include "main.h"
/**
 * main - prints a squareusing the character #
 * Description: prints a squareusing the character #
 * Return: Always 0(success)
 */
int main(void)
{
	print_square(2);
	print_square(10);
	print_square(0);
	return (0);
}

