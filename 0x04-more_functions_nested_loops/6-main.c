#include "main.h"
/**
 * main - draws a straight line in terminal
 * Description: draws a straight line in terminal
 * Return: Always 0(success)
 */
int main(void)
{
	print_line(0);
	print_line(2);
	print_line(10);
	print_line(-4);
	return (0);

